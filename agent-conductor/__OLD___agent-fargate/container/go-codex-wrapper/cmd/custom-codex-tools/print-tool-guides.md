# print-tool-guides

Use this tool to print the quick Markdown guide for every registered custom Codex tool.

Command:

```bash
custom-codex-tools --print-tool-guides
```

Expected behavior:

- Prints each tool's guide.
- Does not start a nested agent.

Use this when unsure which helper tools are available.
