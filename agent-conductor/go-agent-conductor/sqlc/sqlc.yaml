version: "2"
sql:
  - engine: "sqlite"
    schema: "database.sql"
    queries: "queries"
    gen:
      go:
        package: "dbgen"
        out: "../internal/db/generated"
        emit_json_tags: true
