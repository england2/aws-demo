# print-tool-guides

Use this tool to print the quick Markdown guide for every registered
`codex-wrapper` tool.

Command:

```bash
codex-wrapper --print-tool-guides
```

Expected behavior:

- Prints each tool name.
- Prints that tool's guide.
- Does not start a nested agent.

Use this when unsure which helper tools are available.
