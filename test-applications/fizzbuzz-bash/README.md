# Bash FizzBuzz

A small Bash test application that prints FizzBuzz values from 1 through a
positive integer limit.

## Usage

```sh
./fizzbuzz.sh
./fizzbuzz.sh 15
```

When no limit is supplied, the script prints values from 1 through 100.

## Test

```sh
./test_fizzbuzz.sh
```
