# Bash Fun

A small Bash program that prints a randomized tiny adventure for a named player.

## Run

```bash
./fun.sh --name Ada
```

Use `--no-color` for plain output:

```bash
./fun.sh --name Ada --no-color
```

Show all options:

```bash
./fun.sh --help
```
