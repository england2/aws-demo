# Bash Fun

A tiny Bash program that prints a randomized terminal celebration.

Run it directly:

```sh
./fun.sh --no-color
```

Or build and run the container:

```sh
docker build -t bash-fun .
docker run --rm bash-fun
```
